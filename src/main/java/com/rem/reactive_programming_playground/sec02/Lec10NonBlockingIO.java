package com.rem.reactive_programming_playground.sec02;

import com.rem.reactive_programming_playground.common.Util;
import com.rem.reactive_programming_playground.sec02.client.ExternalServiceClient;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Lec10NonBlockingIO {

    private static final Logger logger = LoggerFactory.getLogger(Lec10NonBlockingIO.class);

    public static void main(String[] args) {
      /*  var client = new ExternalServiceClient();

        logger.info("starting to call the external service...");

        for (int i = 0; i < 50; i++) {
            client.getProductName(i)
                    .subscribe(Util.subscriber());
        }

        Util.sleepSeconds(3);
    } */

    }
}
